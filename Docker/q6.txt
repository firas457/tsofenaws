multistage build: build the docker file in stages, FROM... AS name and then continue using the name, FROM name....
this can be very efficinet in memory (Storage space), the image will be built with smaller size.
